setAttendance = SELECT OVERTIME, SFT_ON, SFT_OFF, SFT_ID, ATD_ON, ATD_OFF FROM SHIFT_TB S JOIN ATTENDANCE_TB A USING(SFT_ID) WHERE S.BUS_ID = ? AND A.EMP_ID = ? AND ATD_ON LIKE TO_CHAR(SYSDATE,'yyyymmdd%')